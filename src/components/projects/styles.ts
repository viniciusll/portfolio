import styled from 'styled-components';

export const Container = styled.section`
    height: 100vh;
    margin-top: 10%;
    display: flex;
    flex-direction: column;

    .title {
        color: '#fff'
    }
`;